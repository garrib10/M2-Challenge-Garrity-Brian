  @Configuration
    public class SpringFoxConfig {
    @Bean
    public Docket api() {
    return new Docket(DocumentationType.SWAGGER_2)
    .select()
    .apis(RequestHandlerSelectors.basePackage("com.company.M2ChallengeGarrityBrian"))
    .paths(PathSelectors.any())
    .build();
    }
  }